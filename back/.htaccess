# ---------------------------------------------------------------
# 於 2026-08-14 的安全修復中新增。對應 A01-3。
#
# back/*.php 只會被 admin.php 以伺服器端 include 載入，
# 從來不需要由瀏覽器直接開啟。直接存取一律拒絕。
# ---------------------------------------------------------------
Options -Indexes
Require all denied
