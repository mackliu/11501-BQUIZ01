# ---------------------------------------------------------------
# 於 2026-08-14 的安全修復中新增。對應 A02-2。
#
# 本目錄存放資料庫傾印、遷移腳本與應用程式日誌，
# 全部都不該能被瀏覽器取得。
#
# 註：這是「目前架構下能做到的最佳防護」。真正正確的做法是把整個應用
#     搬成只有 public/ 目錄對外，其餘檔案放在 web root 之外 ——
#     這樣就完全不需要依賴 .htaccess 是否生效。該調整屬第 2 階段待辦。
# ---------------------------------------------------------------
Options -Indexes
Require all denied
